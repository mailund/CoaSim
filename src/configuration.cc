
#include "configuration.hh"


/*
 * After a re-factorization that moved markers elsewhere, the
 * configuration is defined solely in the header file.  The only
 * significant method is the constructure, and that is a template and
 * thus must be visible in the header file.
 */
